<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">

    <!-- Diretório de logs (usa ${catalina.home} quando rodando no Tomcat) -->
    <property name="LOG_HOME" value="${catalina.home}/logs/struts-spring-hello" />

    <!-- Appender para console -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Appender para arquivo -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_HOME}/application.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_HOME}/application.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>10</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Níveis de log específicos -->
    <logger name="org.springframework" level="INFO"/>
    <logger name="org.apache.struts2" level="DEBUG"/>
    <logger name="org.apache.cxf" level="INFO"/>
    <logger name="br.com.wsoubar.hello" level="DEBUG"/>

    <!-- Log raiz -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>
